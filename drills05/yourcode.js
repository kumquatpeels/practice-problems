

function populateRecords(arr){
  // return arr.map(x => Object.values(x))
  // const charge = arr.filter(x => x.type === 'charge')
  // const cash = arr.filter(x => x.type === 'cash advance')
  // let chargeTotal = 0
  // let cashTotal = 0
  // for (var i = 0; i < charge.length; i++) {
  //   chargeTotal += parseFloat(charge[i].amount)
  // }
  // for (var j = 0; j < cash.length; j++) {
  //   cashTotal += parseFloat(cash[j].amount)
  // }
  // return {
  //   'charge': parseFloat(chargeTotal.toFixed(2)),
  //   'cash advance': parseFloat(cashTotal.toFixed(2))
  // }
}
